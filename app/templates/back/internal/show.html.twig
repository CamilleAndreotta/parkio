{% extends 'base.html.twig' %}

{% block title %}InternalLocation{% endblock %}

{% block body %}
    <h1>InternalLocation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ internal_location.id }}</td>
            </tr>
            <tr>
                <th>DateStart</th>
                <td>{{ internal_location.dateStart ? internal_location.dateStart|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateEnd</th>
                <td>{{ internal_location.dateEnd ? internal_location.dateEnd|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Information</th>
                <td>{{ internal_location.information }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_internal_index') }}">back to list</a>

    <a href="{{ path('app_back_internal_edit', {'id': internal_location.id}) }}">edit</a>

    {{ include('back/internal/_delete_form.html.twig') }}
{% endblock %}
