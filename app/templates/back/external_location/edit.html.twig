{% extends 'base.html.twig' %}

{% block title %}Editer un prêt{% endblock %}

{% block body %}

<div class="index__container"> 

    <div class="index__container-title orange">
        <h1>Editer un prêt à un utilisateur externe</h1>
    </div>

    <div class="edit__container-form">
        {{ include('back/external_location/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="edit__container-form">
        {{ include('components/materialListEditExternal.html.twig') }}
    </div>

    <div class="edit__container-form">

        <div class="show__container-btn">
            <a class="btn-list" href="{{ path('app_back_external_location_index') }}">back to list</a>
            {{ include('back/external_location/_delete_form.html.twig') }}
        </div>

    </div>

</div>

{% endblock %}
