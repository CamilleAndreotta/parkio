{% extends 'base.html.twig' %}

{% block title %}Keyboard{% endblock %}

{% block body %}
<div class="index__container"> 

    <div class="index__container-title orange">
        <h1>Clavier</h1>
    </div>

    <div class="show__container-table">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ keyboard.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ keyboard.model }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ keyboard.status }}</td>
            </tr>
        </tbody>
    </table>

    <div class="show__container-btn">

        <a class="btn-list" href="{{ path('app_back_material_keyboard_index') }}">back to list</a>

        <a class="btn-update" href="{{ path('app_back_material_keyboard_edit', {'id': keyboard.id}) }}">edit</a>

        {{ include('back/material/keyboard/_delete_form.html.twig') }}

    </div>

    </div>

</div>
{% endblock %}
