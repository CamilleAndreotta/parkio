{% extends 'base.html.twig' %}

{% block title %}Monitor{% endblock %}

{% block body %}

<div class="index__container"> 

    <div class="index__container-title">
        <h1>Ecran</h1>
    </div>

    <div class="show__container-table">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ monitor.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ monitor.model }}</td>
            </tr>
            <tr>
                <th>PurchaseDate</th>
                <td>{{ monitor.purchaseDate ? monitor.purchaseDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ monitor.status }}</td>
            </tr>
        </tbody>
    </table>

    <div class="show__container-btn">

        <a class="btn-list" href="{{ path('app_back_material_monitor_index') }}">back to list</a>

        <a class="btn-update" href="{{ path('app_back_material_monitor_edit', {'id': monitor.id}) }}">edit</a>

        {{ include('back/material/monitor/_delete_form.html.twig') }}

    </div>

    </div>

</div>
{% endblock %}
