{% extends 'base.html.twig' %}

{% block title %}Edit Monitor{% endblock %}

{% block body %}

<div class="index__container"> 

    <div class="index__container-title orange">
        <h1>Editer l'écran</h1>
    </div>

    <div class="edit__container-form">
        {{ include('back/material/monitor/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="edit__container-form">
        <div class="show__container-btn">
            <a class="btn-list" href="{{ path('app_back_material_monitor_index') }}">back to list</a>

            {{ include('back/material/monitor/_delete_form.html.twig') }}
        </div>
    </div>

</div>

{% endblock %}
