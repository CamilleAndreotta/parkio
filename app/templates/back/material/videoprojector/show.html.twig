{% extends 'base.html.twig' %}

{% block title %}Videoprojector{% endblock %}

{% block body %}
    <h1>Videoprojector</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ videoprojector.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ videoprojector.model }}</td>
            </tr>
            <tr>
                <th>PurchaseDate</th>
                <td>{{ videoprojector.purchaseDate ? videoprojector.purchaseDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ videoprojector.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_material_videoprojector_index') }}">back to list</a>

    <a href="{{ path('app_back_material_videoprojector_edit', {'id': videoprojector.id}) }}">edit</a>

    {{ include('back/material/videoprojector/_delete_form.html.twig') }}
{% endblock %}
