<form method="post" action="{{ path('app_back_material_computer_delete', {'id': computer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ computer.id) }}">
    <button class="btn btn__delete">Supprimer</button>
</form>
