{% extends 'base.html.twig' %}

{% block title %}Mouse{% endblock %}

{% block body %}

<div class="index__container"> 

    <div class="index__container-title">
        <h1>Souris</h1>
    </div>

    <div class="show__container-table">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mouse.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ mouse.model }}</td>
            </tr>
            <tr>
                <th>Affectation</th>
                <td>{{ mouse.affectation }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ mouse.status }}</td>
            </tr>
        </tbody>
    </table>

    <div class="show__container-btn">

        <a class="btn-list" href="{{ path('app_back_material_mouse_index') }}">back to list</a>

        <a class="btn-update" href="{{ path('app_back_material_mouse_edit', {'id': mouse.id}) }}">edit</a>

        {{ include('back/material/mouse/_delete_form.html.twig') }}

    </div>

    </div>

</div>
{% endblock %}
