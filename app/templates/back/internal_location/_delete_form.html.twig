<form method="post" action="{{ path('app_back_internal_location_delete', {'id': internal_location.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ internal_location.id) }}">
    <button class="btn btn__delete">Supprimer</button>
</form>
