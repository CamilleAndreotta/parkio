{% extends 'base.html.twig' %}

{% block title %}Edit InternalLocation{% endblock %}

{% block body %}

<div class="index__container"> 

    <div class="index__container-title">
        <h1>Editer un prêt à un utilisateur interne</h1>
    </div>

    <div class="edit__container-form">
        {{ include('back/internal_location/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="edit__container-form">
        {{ include('components/materialListEditInternal.html.twig') }}
    </div>

    <div class="edit__container-form">

        <div class="show__container-btn">
            <a class="btn-list" href="{{ path('app_back_internal_location_index') }}">back to list</a>

            {{ include('back/internal_location/_delete_form.html.twig') }}
        </div>

    </div>

</div>
{% endblock %}
