{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}

<div class="index__container"> 

    <div class="index__container-title">
        <h1>Ajouter un utilisateur</h1>
    </div>

    <div class="edit__container-form">
        {{ include('back/user/_form.html.twig') }}
    </div>

    <div class="edit__container-form">

        <div class="new__container-btn">
            <a class="btn-list" href="{{ path('app_back_user_index') }}">back to list</a>
        </div>
    
    </div>

</div>

{% endblock %}
